<Window x:Class="DialogueWindows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DialogueWindows"
        mc:Ignorable="d"
        Title="LoginWindow" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="20" />
                <RowDefinition Height="20" />
                <RowDefinition Height="20" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock>Login:</TextBlock>

            <TextBox Name="usernameBox" Grid.Row="1" MinWidth="250">Username</TextBox>
            <TextBox Name="passwordBox" Grid.Row="2" MinWidth="250">Password</TextBox>

            <WrapPanel Grid.Row="3" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button IsDefault="True" Click="Accept_Click" MinWidth="60" Margin="0,0,10,0">Ok</Button>
                <Button IsCancel="True" MinWidth="60">Cancel</Button>
            </WrapPanel>

        </Grid>
    </Grid>
</Window>
