<section id="details-page">
    <h1>Details</h1>
    <article class="details-card">
        <article class="details-card-text">
            <h2>Title:<br> {{item.title}}</h2>
            <h3>Author: {{item.author.username}}</h3>
            <h3>Painting technique: {{item.technique}}</h3>
            <h3>Certificate of authenticity: {{item.certificate}}</h3>
            {{#if item.hasUser}}
                <div class="buttons">
                    {{#if item.isAuthor}}
                        <a href="/item/edit/{{item._id}}" class="btn-edit">Edit</a>
                        <a href="/item/delete/{{item._id}}" class="btn-delete">Delete</a>
                    {{else}}
                        {{#if item.isShared}}
                            <p class="shared-pub">You already shared this publication</p>
                        {{else}}
                            <a href="/item/share/{{item._id}}" class="btn-share">Share publication</a>
                        {{/if}}
                    {{/if}}
                </div>
            {{else}}
            {{/if}}
            <!-- If there is no registered user, do not display buttons-->
            {{!-- <div class="buttons">
                <!-- Only for registered user and author of the publication -->
                <a href="#" class="btn-edit">Edit</a>
                <a href="#" class="btn-delete">Delete</a>
                <!-- logged in user who has not yet shared the publication-->
                <a href="#" class="btn-share">Share publication</a>
                <!-- logged in user who has already shared the publication-->
                <p class="shared-pub">You already shared this publication</p>
            </div> --}}
        </article>
        <article class="details-card-image">
            <img src="{{item.imageUrl}}" alt="art-image">
        </article>
    </article>
</section>
 