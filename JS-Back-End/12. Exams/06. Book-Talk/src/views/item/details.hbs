<section class="details-page">
    <h1>Details</h1>
    <article class="details-card">
        <article class="details-card-text">
            <h2>Title:<br> {{item.title}}</h2>
            <h3>Author: {{item.author}}</h3>
            <h3>Stars: {{item.stars}}</h3>
            <h3>Genre: {{item.genre}}</h3>
            <h3>Review: {{item.review}}</h3>
            {{#if item.hasUser}}
                <div class="buttons">
                    {{#if item.isOwner}}
                        <a href="/item/edit/{{item._id}}" class="btn-edit">Edit</a>
                        <a href="/item/delete/{{item._id}}" class="btn-delete">Delete</a>
                    {{else}}
                        {{#if item.isWished}}
                            <p class="wish-pub">You already added the book to your wish list</p>
                        {{else}}
                            <a href="/item/wish/{{item._id}}" class="btn-wish">Wish to read</a>
                        {{/if}}
                    {{/if}}
                </div>
            {{else}}
            {{/if}}

        </article>
        <article class="details-card-image">
            <img src="{{item.imageUrl}}">
        </article>
    </article>
</section>