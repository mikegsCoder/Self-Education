// Write a simple function that calculates the length of the string.

// ---------- solution: ----------
function stringLength(str: string) {
  console.log(`The length is ${str.length} symbols.`);
}

stringLength('Hello TypeScript!') // The length is 17 symbols.
